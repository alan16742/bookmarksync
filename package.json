{
  "name": "webdav-bookmark-sync",
  "version": "1.0.2",
  "description": "Chrome extension for syncing bookmarks via WebDAV",
  "scripts": {
    "build": "node build.js",
    "generate-key": "node -e \"require('./build.js').generateKey()\""
  },
  "devDependencies": {
    "archiver": "^5.3.1",
    "glob": "^7.2.0"
  },
  "dependencies": {
    "webdav-bookmark-sync": "file:"
  }
}
